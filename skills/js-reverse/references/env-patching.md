# Environment Patching Rules

- Only patch objects that page evidence has proven are needed
- Patch one minimal causal unit at a time
- Patch values first, then function shells, then returned-object contracts
- Re-run after each patch and record whether the first divergence moves earlier
